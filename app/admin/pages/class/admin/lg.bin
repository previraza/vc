<thead>
    <!-- MATIERES LISTES -->
    <?php $COTES = array('credit' => array(),'courses' => array(),'students' => array());
        $total_success = $total_echec = $moyenne_classe = 0;
        $sizsOfGrille = isset($_POST['G_size'])&&$_POST['G_size']!='full'?($_POST['G_size']=='gprs'?" AND (part = 1 OR part = 3 OR part = 5)":($_POST['G_size']=='gdus'?" AND (part = 2 OR part = 4 OR part = 6)":null)):null;
        $rqt = mysqli_query($db, "SELECT * FROM courses WHERE active = 1 AND `delete` = 0 AND class = $class->id $sizsOfGrille ORDER BY part, id");
        $class_course = mysqli_num_rows(mysqli_query($db, "SELECT * FROM courses WHERE active = 1 AND `name` != 'Stage' AND `name` != 'Mémoire' AND `delete` = 0 AND class = $class->id $sizsOfGrille ORDER BY part, id"));
        $repartition_cote = [
            '-'=>(object)['nbr'=>0,'max'=>'-'],
            'AA'=>(object)['nbr'=>0,'max'=>'-'],
            'A'=>(object)['nbr'=>0,'max'=>'-'],
            'SAT'=>(object)['nbr'=>0,'max'=>'-'],
            'DIS'=>(object)['nbr'=>0,'max'=>'-'],
            'G-D'=>(object)['nbr'=>0,'max'=>'-'],
        ];
    ?><tr>
        <th colspan="2" rowspan="3"><?= "$class->level_name | $class->option_name".($class->name ? " - $class->name" : null) ?></th>
        <?= $class_course ? '<th colspan="'.$class_course.'">Matières</th>' : NULL ?>
        <th colspan="<?=$class->terminal?10:6?>" rowspan=2>RESULTATS</th>
        <th rowspan="5" class="del-print">ACTIONS</th>
    </tr>
    <!-- MATIERES LISTES -->
    <?php $all_course = ''; while ($course = mysqli_fetch_object($rqt)){ 
        $rqt_cote = mysqli_query($db, "SELECT `student`, `course`, ROUND(CASE WHEN `RMC` != 0 AND ".(isset($_POST['periode'])&&($_POST['periode']=='gpsn')?0:1).
                " THEN `RMC` ELSE ((`TP1`+`TP2`+`TP3`)/12+(`INT1`+`INT2`)/8+`EXAM`/2) END,2) AS TOTAL, ".(  
                isset($_POST['periode'])&&($_POST['periode']=='gpsn' || $_POST['periode']=='gpsr')?0:'CASE WHEN `RRC` != 0 THEN `RRC` ELSE `RATR` END '
            ). " AS `RATR`, `JURY` FROM cotes WHERE active = 1 AND course = $course->id"
        ) or die(mysqli_error($db));
        while($row_cote = mysqli_fetch_array($rqt_cote)) {
            $COTES['students'][$row_cote['student']][$row_cote['course']] = $row_cote;
        } if($course->name === 'Stage') $stage = $course->id;
        elseif($course->name === 'Mémoire') $memoire = $course->id;
        else {
            $COTES['credit'][$course->id] = $course->credit;
            $all_course.= '<th class=write-mode-tb><span>'.$course->name.'</span></th>';
        } 
    } $all_course.='<th class="text-uppercase write-mode-tb"><span>Echèc Leger</span></th><th class="text-uppercase write-mode-tb"><span>Echec Profond</span></th>
    <th class="text-uppercase write-mode-tb"><span>Nombres de vide</span></th><th class="text-uppercase write-mode-tb"><span>Total Annuel</span></th>' ?>
    <tr>
        <?php for ($i=1; $i <= $class_course; $i++) echo '<th>'.$i.'</th>'?>
    </tr>
    <tr>
        <?php for ($i=1; $i <= $class_course; $i++) echo '<th>20</th>'?>
        <th colspan=2 rowspan=2><?= $class_course ?></th>
        <th rowspan=2><?= $class_course ?></th>
        <th rowspan=2><?= $class_res_tot = ($credits = array_sum($COTES['credit'])) * 20 ?></th>
        <th rowspan=2><?=$class->terminal?50:100?>%</th>
        <?php if ($class->terminal) {?>
            <th colspan=2>50%</th>
            <th rowspan=2><?= $class_res_tot*2 ?></th>
            <th rowspan=2>100%</th>
        <?php }?>            
        <th class="text-uppercase write-mode-tb" rowspan=3><span>Decision du Jury</span></th>
    </tr>
    <tr>
        <th rowspan=2>N°</th>
        <th class="text-left">Pondération</th>
        <?php foreach($COTES['credit'] AS $credit) echo '<th>'.$credit.'</th>'?>
        <?php if ($class->terminal) {?>
            <th>20%</th>
            <th>30%</th>
        <?php }?> 
    </tr>
    <tr>
        <th class="text-left">Etudiants</th>
        <?= $all_course;?>
        <th class="text-uppercase write-mode-tb"><span>Resultat Annuel</span></th>
        <?php if ($class->terminal) {?>
            <th class="text-uppercase write-mode-tb"><span>Moyenne Stage</span></th>
            <th class="text-uppercase write-mode-tb"><span>Moyenne Mémoire</span></th>
            <th class="text-uppercase write-mode-tb"><span>Total Generale </span></th>
            <th class="text-uppercase write-mode-tb"><span>Résultat Finale</span></th>
        <?php }?> 
    </tr>
</thead>
<tbody>
    <?php $periode = ($_POST['periode']??NULL) == 'gpsr' ? "finish_in = 'gpsr' OR finish_in = 'gdsn' OR finish_in = 'gdsr'" : 
                    (($_POST['periode']??NULL) == 'gdsn' ? "finish_in = 'gdsn' OR finish_in = 'gdsr'" :
                    (($_POST['periode']??NULL) == 'gdsr' ? "finish_in = 'gdsr'" : 1)) ;
            $rqt_std = mysqli_query($db, "SELECT students.*, palmares.completed, palmares.resultat, 
                                    (SELECT DISTINCT cotes.student FROM cotes WHERE cotes.RMC AND cotes.student = students.id) AS RPS,
                                    (SELECT DISTINCT cotes.student FROM cotes WHERE cotes.RRC AND cotes.student = students.id) AS RDS
                                FROM students ".(isset($_POST['periode'])&&($_POST['periode']=='RSR' || $_POST['periode']=='ASR')?NULL:'LEFT')." JOIN palmares ON palmares.student = students.id WHERE (finish_in IS NULL OR $periode) AND students.active = 1 AND students.delete = 0 AND students.class = $class->id ORDER BY firstname, lastname".(isset($std_trancon)&&$std_trancon!=0?' LIMIT 100 OFFSET '.(--$std_trancon):null));
        $count = 0;
        $all_count_std = mysqli_num_rows($rqt_std);
        while($row_std = mysqli_fetch_array($rqt_std)) {
            $std = (object) $row_std;
            if (isset($_POST['periode'])&&($_POST['periode']=='RSR' || $_POST['periode']=='ASR') && $std->completed == 1 && $std->resultat > 50) continue;
            $success = $np = $credits = $cote_credit = 0 ; $cote = array();
            $echecs = ['l'=>0,'g'=>0];
        ?><tr class="delibereBtn" data-rps=<?=$std->rps??''?> data-rds=<?=$std->rds??''?> id='std_<?= ($std->id) ?? NULL ?>'> 
            <!-- DATA STUDENT -->
            <td><?= ++$count ?></td>
            <td class="text-left"><?= "$std->firstname $std->lastname" ?></td>

            <!-- RESULT STUDENT -->
            <?php foreach($COTES['credit'] AS $course => $credit) {
                $jury=$cote=$codeIsset=$stage_codeIsset=$memoire_codeIsset=0;
                if(isset($COTES['students'][$std->id][$course])){
                    $codeIsset = 1;
                    $pre_cote = $COTES['students'][$std->id][$course]['TOTAL']+0;
                    $cote = ($COTES['students'][$std->id][$course]['RATR']??0) > $pre_cote ? $COTES['students'][$std->id][$course]['RATR'] : $pre_cote ;
                    $jury = $COTES['students'][$std->id][$course]['JURY']??0;
                } else $np++; if($cote < 10 && $jury < 10) $echecs[($cote>=8||$jury>=8)?'l':'g']++ ; else {$success++; $credits+=$credit;}
                $cote_credit += ($final = ($jury&&$jury!=$cote?$jury:$cote))*$credit;
                echo "<th data-student='$std->id' data-course='$course'  data-isset=$codeIsset data-jury='$jury' data-cote='$cote'".($jury && $jury != $cote?"data-toggle=tooltip data-placement=top data-original-title='$cote' data-simbing":null).' class="text-center '.($final < 10 ? 'text-danger' : 'text-success').'">'.$final.'</th>';
            } 
            if(isset($stage, $COTES['students'][$std->id][$stage])){
                $stage_codeIsset=1;
                $stage_cote = (($stage_pre_cote = $COTES['students'][$std->id][$stage]['TOTAL']+0) < 10 && $COTES['students'][$std->id][$stage]['RATR']) ? $COTES['students'][$std->id][$stage]['RATR'] : $stage_pre_cote ;
                $stage_jury = $stage_jury = $COTES['students'][$std->id][$stage]['JURY']; $stage_final = ($stage_jury&&$stage_jury!=$stage_cote?$stage_jury:$stage_cote);
            } else $stage_cote = $stage_jury = $stage_final = $stage_jury = 0;
            $stage_res = '<th data-limit=30 data-student='.$std->id.' data-isset='.$stage_codeIsset.' data-jury="'.$stage_jury.'" data-course='.($stage??0).' data-cote='.$stage_cote.($stage_jury && $stage_jury != $stage_cote?"data-toggle=tooltip data-placement=top data-original-title='$stage_cote' data-simbing":null).' class="text-center '.($stage_final < 10 ? 'text-danger' : 'text-success').'">'.$stage_final.'</th>';

            if (isset($memoire, $COTES['students'][$std->id][$memoire])) {
                $memoire_codeIsset=1;
                $memoire_cote = (($memoire_pre_cote = $COTES['students'][$std->id][$memoire]['TOTAL']+0) < 10 && $COTES['students'][$std->id][$memoire]['RATR']) ? $COTES['students'][$std->id][$memoire]['RATR'] : $memoire_pre_cote ;
                $memoire_jury = $memoire_jury = $COTES['students'][$std->id][$memoire]['JURY']; $memoire_final = ($memoire_jury&&$memoire_jury!=$memoire_cote?$memoire_jury:$memoire_cote);
            } else $memoire_cote = $memoire_jury = $memoire_final = $memoire_jury = 0;
            $memoire_res = '<th data-limit=30 data-student='.$std->id.' data-isset='.$memoire_codeIsset.' data-jury="'.$jury.'" data-course='.($memoire??0).' data-cote='.$memoire_cote.($memoire_jury && $memoire_jury != $memoire_cote?"data-toggle=tooltip data-placement=top data-original-title='$memoire_cote' data-simbing":null).' class="text-center '.($memoire_final < 10 ? 'text-danger' : 'text-success').'">'.$memoire_final.'</th>';
            ?>

            <!-- RESULT CALC -->
            <td><?= $echecs['l'] ?></td>
            <td><?= $echecs['g'] ?></td>
            <td><?= $np ?></td>
            <td><?= $cote_credit = ($cote_credit) ?></td>
            <td><?= ($res = number_format(($cote_credit*100)/$class_res_tot, 1))<40&&!$np?'-':number_format($res/($class->terminal?2:1),1) ?> %</td>
            <?php if ($class->terminal) {?>
                <?= $stage_res.$memoire_res ?>
                <td><?= $final_code = ($cote_credit+(($stage_final+$memoire_final)*$class_res_tot/50)) ?></td>
                <td><?= ($res = number_format(($final_code*100)/($class_res_tot*2), 1))<40&&!$np?'-':$res ?> %</td>
                <?php } ?>
                <td><?php $mention = $res<40&&!$np?'-':($np == 0 && $res < 50 ? 'AA' : (($echecs['g'] || $echecs['l'] > 3 || ($echecs['l'] == 3 && $res < 60) || ($echecs['l'] == 2 && $res < 55) || ($echecs['l'] == 1 && $res < 50)) ? 'A' : (($res >= 85) ? 'G-D' : (($res >= 70) ? 'DIS' : (($res >= 50) ? 'SAT' : 'AA'))))) ?></td>
            <td class="del-print lastTd" data-periode="<?=$_POST['periode']??'full'?>" data-ended=<?=($mention=='SAT'||$mention=='DIS'||$mention=='G-D')&&$np==0?1:0?> data-completed=<?=$mention=='SAT'||$mention=='DIS'||$mention=='G-D'?1:0?> data-student=<?=$std->id?> data-resultat=<?=$res?> data-mention=<?=$mention?>>
                <div class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown" aria-expanded="false" aria-label="Plus d'option">
                        <span class="flaticon-more-button-of-three-dots"></span>
                    </a>
                    <div class="dropdown-menu dropdown-menu-right">
                        <a class="dropdown-item" href="?fa69951bc733eb82713e936be33597e1=DATA_CRYPTING_ON:fa69951bc733eb82713e936be33597e1=WABTECHS_CRYPTING_ON: e91497f38407364c8e86ade9098950d978814190&MCISME_LAVINGOL&b557c222c8ddcdf87e033706aad08e4ef7cf90d3MCISME_LAVINGOL&b557c222c8ddcdf87e033706aad08e4ef7cf90d3VIRAZA_LAVINGOL&b557c222c8ddcdf87e033706aad08e4ef7cf90d3&MCISME_LAVINGOL&b557c222c8ddcdf87e033706aad08e4ef7cf90d3&WABTECHS_CRYPTING_ON_LAVINGOL&MCISME_LAVINGOL&b557c222c8ddcdf87e033706aad08e4ef7cf90d3&b557c222c8ddcdf87e033706aad08e4ef7cf90d3=student--details--<?= $std->id ?>"><i class="fas fa-cogs text-dark-pastel-green"></i>Profile</a>
                        <a class="dropdown-item" data-exec="backStudent(<?= $std->id ?>)"><i class="fas fa-left-long text-primary"></i>Back</a>
                        <a class="dropdown-item" data-exec="backStudent(<?= $std->id ?>, 'gpsn')"><i class="fas fa-left-long text-primary"></i>Back (S1)</a>
                        <a class="dropdown-item" data-exec="backStudent(<?= $std->id ?>, 'gpsr')"><i class="fas fa-left-long text-primary"></i>Back (S1R)</a>
                        <a class="dropdown-item" data-exec="backStudent(<?= $std->id ?>, 'gdsn')"><i class="fas fa-left-long text-primary"></i>Back (S2)</a>
                        <a class="dropdown-item" data-exec="backStudent(<?= $std->id ?>, 'gdsr')"><i class="fas fa-left-long text-primary"></i>Back (S2R)</a>
                        <a class="dropdown-item" data-exec="open('/app/admin/pages/actions/student-resultat?code=<?= $std->key ?>&anacad=2022')"><i class="fas fa-redo-alt text-orange-peel"></i>Bulletin</a>
                    </div>
                </div>
            </td>
        </tr>
    <?php
    ($res>=50)?$total_success++:$total_echec++; $moyenne_classe+=$res/$all_count_std;
    $repartition_cote[$mention]->nbr++;
    if($repartition_cote[$mention]->max<$res) $repartition_cote[$mention]->max=$res;
} ?>
</tbody>
